import styled from "styled-components";

function Order() {
    return (
        <OrderStyle>
            <div>Order</div>
        </OrderStyle>
    );
}

const OrderStyle = styled.div``;

export default Order;
